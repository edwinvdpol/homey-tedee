{
  "triggers": [
    {
      "$extends": "connected_false"
    },
    {
      "$extends": "connected_true"
    },
    {
      "$extends": "update_available_true"
    }
  ],
  "conditions": [
    {
      "$extends": "connected"
    },
    {
      "$extends": "update_available"
    }
  ]
}
